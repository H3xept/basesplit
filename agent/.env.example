# XMTP Configuration
PRIVATE_KEY=0x... # Your wallet private key for the agent
XMTP_ENV=local # local, dev, or production
DB_PATH=/app/data/xmtp.db3 # Optional: Path to XMTP database (for Railway volumes)

# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
# Note: The old Vision API key can still work, but Gemini API key is recommended
GOOGLE_API_KEY=your_gemini_api_key_here
GOOGLE_VISION_API_KEY=your_api_key_here  # Legacy fallback, will use GOOGLE_API_KEY if set

# Miniapp URL
MINIAPP_URL=http://localhost:3000

# Database Configuration
# PostgreSQL connection string (required for production)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://localhost:5432/basesplit

# Node Environment
NODE_ENV=development
