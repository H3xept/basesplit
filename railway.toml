[build]
builder = "NIXPACKS"
buildCommand = "cd agent && npm install && npm run build"

[deploy]
startCommand = "cd agent && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
